pipeline {
    
    @Library('piper-lib-os') _
    
    agent any
    
    stages {
        
        stage("build") {

            steps {
                echo 'Building the application...!'
            }
        }
    }
}
